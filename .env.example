# ─── Database (Docker Compose — local only) ──────────────────
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_USER=observatory
DATABASE_PASSWORD=observatory_secret
DATABASE_NAME=observatory_db

# ─── Database (Railway / PaaS — replaces all DB_* above) ─────
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ─── Backend ─────────────────────────────────────────────────
NODE_ENV=development
PORT=5000
JWT_SECRET=change-me-in-production

# Comma-separated list of allowed frontend origins
# Local:      http://localhost:5173
# Production: https://your-app.vercel.app
CORS_ORIGIN=http://localhost:5173

# ─── AI (Groq) ──────────────────────────────────────────────
GROQ_API_KEY=
GROQ_MODEL=qwen/qwen3-32b

# ─── Super Admin Seed ────────────────────────────────────────
SUPER_ADMIN_EMAIL=admin@iset-tozeur.tn
SUPER_ADMIN_PASSWORD=Admin@123!

